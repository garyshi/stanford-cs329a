# CS329A 课程笔记工作约定

本目录存放 Stanford CS329A《Self-Improving AI Agents》的中文课程笔记。整理后续讲座时，沿用 [第 1 讲笔记](01-course-overview.md) 的深度与风格，但以该讲实际内容决定章节，不套固定标题。

## 取材顺序

1. 核对用户提供的视频 ID、讲座标题、讲次、主讲人和所属学期；优先参考[课程官网](https://cs329a.stanford.edu/)与视频发布者信息。
2. **视频的音频与展示的幻灯片是一手材料；字幕是定位内容的工具。**先通读带时间戳的字幕或转录，再回看关键片段与幻灯片，尤其核对术语、数字、图表、说话人和字幕听不清的地方。官方字幕也可能有误。
3. 主动检索 Sparse Notes、个人课程笔记或其他 teaching companion，优先选择明确说明取材自完整视频、字幕和指定论文，且抽查后看起来基本正确的笔记。用这些第三方笔记发现字幕遗漏或误听、补充容易忽略的课堂问答与论文细节、借鉴更清晰的组织方式，并寻找有解释价值的幻灯片或论文图表。第三方笔记始终属于二手材料；吸收其中的观点、数字、截图线索或冲突说明前，须回到视频音频、原始幻灯片或论文核实。区分讲师原意、第三方作者的归纳和自己的延伸解读；引用第三方图片或观点时标明来源，并在资料列表说明该笔记具体用于发现或补充了什么。若没有找到看起来基本可靠的第三方笔记，或无法访问其完整内容，明确记录这一情况，不用低质量摘要凑数。
4. 官网用于核对课程安排、论文及讲师资料；论文原文用于核对具体实验结果。不要把讲师的概述或课堂推测写成论文已经证明的结论。
5. 若资料相互冲突，先找对应视频片段、幻灯片或论文核实；仍无法确认时，在正文标注“待核实”或“讲座中未明确说明”，并写清冲突或缺失之处，不把猜测当结论。视频无法可靠读取时，说明可核对到的范围；不要凭标题补写讲座内容。

### 获取视频字幕

1. 先查看 YouTube 视频页面是否提供字幕或转录，以及课程官网是否链接讲义、幻灯片或逐字稿。记录视频 ID，后续时间链接都使用同一个 ID。
2. 若本机装有 `yt-dlp`，可先检查可用字幕语言：

   ```sh
   yt-dlp --list-subs 'https://www.youtube.com/watch?v=VIDEO_ID'
   ```

   有英文字幕时，优先下载发布者提供的字幕；仅有自动字幕时，再使用自动字幕，并标注其识别误差。示例命令（在临时目录运行，替换视频 ID）：

   ```sh
   yt-dlp --skip-download --write-subs --write-auto-subs --sub-langs en --sub-format vtt 'https://www.youtube.com/watch?v=VIDEO_ID'
   ```

3. 若 YouTube 页面或命令行无法获取字幕，检索该视频 ID 对应的公开字幕索引。逐段阅读带时间戳的转录，用原视频、幻灯片、课程官网或论文核对重点；记录所用索引链接。不要仅依赖自动生成的摘要。
4. 字幕用于定位和理解，成稿应自行概括，不要粘贴大段逐字稿。时间戳从实际字幕或视频核对，字幕遗漏、误听或说话人混淆处避免下确定结论。

## 笔记结构

- 文件名采用 `NN-short-topic.md`，讲次用两位数字。笔记行文使用中文，但技术术语以英文为主（如 `inference-time scaling`、`verifier`、`pass@k`），必要时在括号中给中文参考译名；不要只写中文术语，让读者再反向猜英文。标题、表格、图注和复习问题也遵守此约定。常见英文缩写首次出现时写出英文全称，之后可直接使用缩写；普通叙述词不必机械地双语标注。
- 开头记录课程、学期、主讲人、原视频与课程官网链接，并说明是归纳笔记而非逐字稿。
- 先用一小段写出该讲的**核心主线**，随后按讲座的逻辑分节解释机制、例子、关键区分和局限。适合比较的内容用表格；确实有助于理解的流程或关系图优先用 Mermaid（`mermaid` 代码块），简单关系可直接用文字说明。
- Mermaid 必须采用 Obsidian 内置 Mermaid 能稳定解析的保守语法。优先使用 `flowchart LR`／`flowchart TD`、`A --> B`、`A -->|标签| B` 和无标签虚线 `A -.-> B`；节点文字含 `@`、括号、冒号、斜线或其他特殊字符时写成 `A["pass@k"]` 这类带引号形式。避免依赖较新版本才支持的语法、`@{ ... }` 节点扩展，以及 `A -. 标签 .-> B` 这类在部分 Obsidian 版本中会解析失败的虚线标签写法。确需给虚线关系加说明时，把说明写进目标节点或改用普通带标签箭头。
- 关键段落提供可点击的视频时间链接：`https://www.youtube.com/watch?v=VIDEO_ID&t=SECONDSs`。时间应对应所述内容，不要把整节都指向视频开头。
- 幻灯片截图只选能解释机制、比较或实验结果的少量图片，放在相关段落旁，附简短读图说明与来源。核对坐标轴、图例、指标、数据集及实验限制；图片链接若依赖外部网站，在笔记中保留出处。
- 末尾列出 3–5 个需要主动回答的复习问题，以及原视频、官网、转录和用到的论文链接。
- 将课程行政信息与技术内容分开；截止日期、评分和政策须标明所属学期，避免把历史安排写成当前要求。

## 准确性检查

- 区分训练时改进、推理时扩展和部署后的 Agent 反馈循环；区分模型评审与可执行的外部验证。
- 写清指标的含义与前提。例如 `pass@k` 表示 k 个候选中至少一个正确，不能在没有可靠选择机制时等同于最终单次输出质量。
- 对“涌现”“自我改进”“自主”等宽泛说法注明具体实验或系统机制，避免暗示普遍保证。
- 保留重要成本、失败模式与未解决问题；数字、模型比较和论文结论若未核实，就删去或标明不确定。
- 写完后检查 Markdown 和 Mermaid 渲染、标题层级、链接与时间戳，并确保笔记能独立阅读。Mermaid 至少要在 Obsidian 中实际预览；若当前环境无法打开 Obsidian，应按上述兼容语法做静态检查，并明确说明未完成实际渲染验证。

## Git 提交

如需提交，commit message 除主题行外还应包含说明变更内容的摘要，不要只写一行。
